import React from 'react';import {BrandLogo} from "@/_components/Brand";import Link from "next/link";interface FooterHeadingProps {    description?: string;    href?: boolean;}export const FooterHeading: React.FC<FooterHeadingProps> = ({description, href = false}) => {    return (        <div className="space-y-4">            {href ? <Link href='/'><BrandLogo/></Link> : <BrandLogo/> }            <p className="text-sm text-primary-foreground/85 leading-6">                {description}            </p>        </div>    );};
import React from 'react';

const NavigationBar = () => {
    return (
        <div className="text-primary-foreground">
            Navigation bar
        </div>
    );
};

export default NavigationBar;
